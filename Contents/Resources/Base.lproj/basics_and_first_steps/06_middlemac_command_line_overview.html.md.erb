---
title:  Middlemac command line overview
blurb:  Learn the basics of using the <code>middlemac.rb</code> command-line
        tool.
layout: template-logo-medium
order: 6
---
<%= partial 'markdown-links' %>
<%= partial 'markdown-images' %>

<%= current_page.data.title %>
==============================

<%= current_page.data.blurb %>

Middlemac command signature
---------------------------

`./middlemac.rb TARGET... | all  [--verbose | -v] [--quiet | -q] [--help | -h] [--server | -s]`


Getting help and identifying valid targets
------------------------------------------

If you run _Middlemac_ with an invalid target, with no parameters, or with the
`-h` or `--help` switch, _Middlemac_ will display its documentation.
 
General
-------

You can run _Middlemac_ in server mode by specifying a target and the  `-s`
switch.

You can build one or more targets by specifying one or more target names, or
`all` to build all targets.
